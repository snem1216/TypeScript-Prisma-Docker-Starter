{
  "name": "typescript-base-project",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc",
    "start": "npx tsc && node ./dist/index.js",
    "migrate:start:prod": "prisma migrate deploy && npm run start:prod",
    "start:prod": "node dist/index.js"
  },
  "devDependencies": {
    "prisma": "^5.12.1",
    "typescript": "^5.4.4"
  },
  "dependencies": {
    "@prisma/client": "^5.12.1"
  }
}
